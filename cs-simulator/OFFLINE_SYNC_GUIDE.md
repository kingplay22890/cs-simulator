# Offline-First Синхронизация 📱

## Проблема
У вас медленный интернет, поэтому симуляция матчей с сохранением в БД Supabase идёт очень долго.

## Решение
Теперь приложение работает в **offline-first** режиме:

1. **Все данные сохраняются в localStorage** - быстро, без интернета
2. **Изменения ставятся в очередь** - даже если интернет упал
3. **Вы сами решаете когда синхронизировать** - нажмите кнопку или включите автосинхронизацию

## Как пользоваться

### Вариант 1: Ручная синхронизация (рекомендуется при медленном интернете)

1. Играйте матчи, обновляйте рейтинги - всё сохраняется локально ⚡
2. Когда интернет хороший, нажмите кнопку **💾 N ожидают** в шапке страницы
3. В модальном окне нажмите **🚀 Синхронизировать сейчас**
4. Дождитесь завершения синхронизации

### Вариант 2: Автоматическая синхронизация

1. Откройте модальное окно синхронизации
2. Включите переключатель **Автоматическая синхронизация**
3. Приложение будет автоматически отправлять данные в фоне каждые 5 секунд

## Что синхронизируется

- ✅ Обновления рейтингов игроков
- ✅ Результаты матчей
- ✅ Обновления профилей команд
- ✅ История матчей

## Статус синхронизации

- **💾 N ожидают** - в очереди ждут N изменений
- **📦 Pending list** - детальный список очереди
- **✅ Успешно** - все данные отправлены
- **⚠️ Ошибка** - проблема при отправке (изменения остаются в очереди для повтора)

## Технические детали

### localStorage структура
```
cs_pending_sync - массив JSON объектов с ожидающими изменениями
```

### Пример очереди
```javascript
[
  {
    id: "abc123",
    type: "updatePlayerStats",
    timestamp: 1704085200000,
    data: {
      playerName: "Player1",
      stats: { wins: 10, rating: 1800 }
    }
  }
]
```

### API изменений

```javascript
// Добавить изменение в очередь
window.SyncManager.queueChange(type, data);

// Получить все ожидающие изменения
window.SyncManager.getPending();

// Получить количество ожидающих
window.SyncManager.getPendingCount();

// Синхронизировать сейчас
await window.SyncManager.syncToDatabase();

// Включить/отключить автосинхронизацию
window.SyncManager.setAutoSync(true/false);

// Подписаться на события синхронизации
window.SyncManager.onSyncChange(callback);

// Очистить очередь (осторожно!)
window.SyncManager.clearPending();
```

## Часто задаваемые вопросы

**Q: Что если я закрою приложение?**
A: Все данные остаются в localStorage, они не потеряются. При следующем открытии приложения вы сможете синхронизировать.

**Q: Можно ли потерять данные?**
A: Маловероятно. localStorage хранит данные на вашем компьютере. Они потеряются только если вы очистите кеш браузера.

**Q: Что если сеть упадёт во время синхронизации?**
A: Синхронизация остановится, и изменения останутся в очереди для повторной попытки.

**Q: Почему кнопка синхронизации не видна?**
A: Она появляется только когда в очереди есть ожидающие изменения.

**Q: Как узнать статус синхронизации?**
A: Откройте модальное окно (нажмите на кнопку синхронизации). Там вы увидите количество ожидающих изменений и детальный список.

## Отключение синхронизации (если понадобится)

Если вам нужна старая логика (сохранение прямо в БД):

1. В `api.js` раскомментируйте строки с прямыми вызовами (сейчас закомментированы в `updatePlayerStats`)
2. Удалите или отключите `sync-manager.js`
3. Удалите код синхронизации из UI

## Производительность

- **Локальное сохранение**: ~1ms
- **Синхронизация с БД**: зависит от скорости интернета, но срабатывает асинхронно и не блокирует UI
