## 🏗️ Архитектура Offline-First Синхронизации

### Общая схема

```
┌─────────────────────────────────────────────────────────────┐
│                    UI Layer (HTML pages)                     │
│  tournament-view.html, ratings.html, player-profile.html    │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────┐
│              SyncManager (sync-manager.js)                   │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ • Управление очередью (localStorage: cs_pending_sync)│  │
│  │ • Синхронизация с БД                                │  │
│  │ • Автосинхронизация в фоне                          │  │
│  │ • События синхронизации                             │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────┬─────────────────────────────────────────────┘
                 │
       ┌─────────┴──────────┐
       ▼                    ▼
  ┌──────────┐         ┌──────────┐
  │localStorage       │  Supabase
  │cs_tournaments_v2  │  API
  │cs_teams           │  (Slow - Optional)
  │cs_pending_sync    │
  └──────────┘         └──────────┘
```

### Поток данных

#### 1. Сохранение (Fast Path - всегда используется)
```
User Action (Play Match, Update Rating)
    ↓
tournament-manager.js / ratings.js
    ↓
Функции вроде updatePlayerStats()
    ↓
Если нет интернета ИЛИ ошибка → SyncManager.queueChange()
    ↓
localStorage: cs_pending_sync
    ↓
✅ Данные сохранены локально (instantly)
    ↓
UI обновляется (button count + list)
```

#### 2. Синхронизация (Slow Path - по требованию)
```
Пользователь нажимает "🚀 Синхронизировать сейчас"
    ↓
SyncManager.syncToDatabase()
    ↓
Для каждого изменения в очереди:
    ├→ updatePlayerStats → window.csApi.updatePlayerStats()
    ├→ savePlayerMatch → window.csApi.savePlayerMatch()
    └→ updateTeam → window.csApi.updateTeam()
    ↓
Если успешно → удалить из очереди
Если ошибка → оставить в очереди для повтора
    ↓
Показать результат
```

### Компоненты

#### 1. `sync-manager.js`
**Функции:**
- `queueChange(type, data)` - добавить изменение в очередь
- `getPending()` - получить все ожидающие изменения
- `getPendingCount()` - количество ожидающих
- `syncToDatabase()` - синхронизировать с БД (async)
- `setAutoSync(enabled)` - включить/отключить автосинхронизацию
- `onSyncChange(callback)` - подписаться на события

**localStorage структура:**
```javascript
// cs_pending_sync
[
  {
    id: "unique_id",
    type: "updatePlayerStats",
    timestamp: 1704085200000,
    data: { playerName: "Player1", stats: {...} }
  },
  {
    id: "another_id",
    type: "savePlayerMatch",
    timestamp: 1704085205000,
    data: { player_name: "Player2", ... }
  }
]
```

#### 2. `api.js` (модифицирован)
**Изменения:**
- `updatePlayerStats()` - теперь очередирует при ошибке/отсутствии клиента
- `savePlayerMatch()` - теперь очередирует при ошибке/отсутствии клиента

**Логика:**
```javascript
async function updatePlayerStats(playerName, stats) {
  if (!client) {
    // Нет подключения → очередировать
    window.SyncManager.queueChange('updatePlayerStats', { playerName, stats });
    return null;
  }
  
  try {
    // Попытка отправить в БД
    const { data, error } = await client.from('player_stats').upsert(...);
    
    if (error) {
      // Ошибка → очередировать для повтора
      window.SyncManager.queueChange('updatePlayerStats', { playerName, stats });
    }
    
    return data ? data[0] : null;
  } catch (e) {
    // Исключение → очередировать для повтора
    window.SyncManager.queueChange('updatePlayerStats', { playerName, stats });
    return null;
  }
}
```

#### 3. UI Компоненты (tournament-view.html, ratings.html)
**Элементы:**
- `#syncStatusBtn` - кнопка со статусом (видна только если есть ожидающие)
- `#syncModal` - модальное окно синхронизации
- `#pendingList` - список очереди
- `#autoSyncToggle` - включение автосинхронизации
- `#syncNowBtn` - кнопка синхронизации

**События:**
```javascript
window.SyncManager.onSyncChange(() => {
  updateSyncUI(); // Обновить UI при изменении очереди
});
```

### Сценарии использования

#### Сценарий 1: Медленный интернет
```
1. User играет матч → данные в localStorage ⚡
2. Интернет медленный → API попытается, не получится → очередировать
3. User ждёт хорошего интернета
4. User нажимает "Синхронизировать" → все отправляется быстро
```

#### Сценарий 2: Интернет упал
```
1. User играет матч → API недоступно → очередировать ⚡
2. Интернет вернулся
3. Если включена автосинхронизация → синхронизация произойдёт автоматически
4. Если нет → User нажимает кнопку
```

#### Сценарий 3: Быстрая работа без БД
```
1. User просто хочет играть быстро
2. Отключает автосинхронизацию
3. Все сохраняется локально
4. Когда готов → синхронизирует вручную
```

### Состояния синхронизации

```
┌──────────────────────────────────────────┐
│         NO PENDING CHANGES                │
│  (Button not visible, UI normal)         │
└──────────────────────────────────────────┘
            ▲                    │
            │                    │ User plays / updates
            │                    ▼
            │  ┌──────────────────────────────────────────┐
            │  │  PENDING CHANGES QUEUED                  │
            │  │  • Button visible: "💾 3 ожидают"         │
            │  │  • Auto-sync (if enabled) or manual sync │
            │  │  • List shows pending operations          │
            └─┤                                            │
               └──────────────────────────────────────────┘
                        │
                        │ User clicks "Sync Now"
                        ▼
            ┌──────────────────────────────────────────┐
            │         SYNCING IN PROGRESS              │
            │  • Button disabled                        │
            │  • Shows: "⏳ Синхронизация..."            │
            │  • Uploading to Supabase                 │
            └──────────────────────────────────────────┘
                        │
              ┌─────────┴──────────┐
              ▼                    ▼
    ✅ SUCCESS              ⚠️ ERROR
    • Removed from queue    • Remains in queue
    • Show: "✅ N synced"    • Show error message
    • UI updates            • Retry later
```

### Производительность

| Операция | Время | Примечание |
|----------|-------|-----------|
| Сохранение в localStorage | ~1ms | Мгновенно |
| UI обновление | ~5ms | Синхронное |
| Синхронизация (1 запрос) | 500-5000ms | Зависит от сети |
| Синхронизация (10 запросов) | 5-50s | Может быть медленно |

### Обработка ошибок

```javascript
try {
  // Отправить в БД
} catch (error) {
  // 1. Логировать ошибку
  console.error('Error:', error);
  
  // 2. Очередировать для повтора
  SyncManager.queueChange(type, data);
  
  // 3. Пользователь может попробовать позже
}
```

**Типы ошибок:**
- `Network Error` → очередировать, повторить позже
- `Authentication Error` → показать сообщение (проблема конфигурации)
- `Validation Error` → показать пользователю, не повторять
- `Database Error` → очередировать, повторить позже

### Безопасность

⚠️ **Важно:** 
- localStorage виден всем расширениям браузера
- Конфиденциальные данные НЕ должны быть в очереди
- HTTPS рекомендуется для Supabase
- Обновите `js/config.js` с правильным SUPABASE_URL и SUPABASE_ANON_KEY

### Расширение функциональности

Чтобы добавить новый тип синхронизации:

```javascript
// 1. В api.js добавьте обработку ошибки:
if (error) {
  window.SyncManager.queueChange('myNewType', {
    param1: value1,
    param2: value2
  });
}

// 2. В sync-manager.js добавьте case:
switch (change.type) {
  case 'myNewType':
    if (window.csApi.myNewFunction) {
      const result = await window.csApi.myNewFunction(
        change.data.param1,
        change.data.param2
      );
      if (result !== null) {
        synced++;
      }
    }
    break;
}

// 3. Экспортируйте функцию в api.js:
window.csApi.myNewFunction = myNewFunction;
```

### Тестирование

```javascript
// В консоли браузера (F12):

// 1. Проверить очередь:
console.log(window.SyncManager.getPending());

// 2. Добавить тестовое изменение:
window.SyncManager.queueChange('test', { data: 'hello' });

// 3. Проверить статус:
console.log(window.SyncManager.getPendingCount());

// 4. Включить автосинхронизацию:
window.SyncManager.setAutoSync(true);

// 5. Синхронизировать вручную:
await window.SyncManager.syncToDatabase();

// 6. Подписаться на события:
window.SyncManager.onSyncChange(() => {
  console.log('Sync status changed!');
});
```

---

**Диаграмма кода:**

```
tournament-view.html
├── Включает sync-manager.js
├── Включает api.js (модифицированный)
├── Модальное окно синхронизации
└── JS код управления UI

ratings.html
├── Включает sync-manager.js
├── Включает api.js (модифицированный)
├── Кнопка синхронизации в заголовке
└── JS код управления UI

api.js (Изменён)
├── updatePlayerStats() → очередирует при ошибке
├── savePlayerMatch() → очередирует при ошибке
└── Остальное без изменений

sync-manager.js (НОВЫЙ)
├── SyncManager.queueChange()
├── SyncManager.syncToDatabase()
├── SyncManager.setAutoSync()
├── SyncManager.onSyncChange()
└── localStorage: cs_pending_sync
```

---

**Примечания для разработчиков:**

1. **Auto-sync интервал:** 5000ms (измените в sync-manager.js)
2. **localStorage ключ:** `cs_pending_sync` (не меняйте)
3. **События:** используйте `onSyncChange()` для реактивного UI
4. **Батчинг:** можно добавить батчинг запросов (пока нет)
5. **Статистика:** можно добавить метрики синхронизации (пока нет)
